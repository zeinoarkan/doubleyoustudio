<nav class="fixed top-0 left-0 w-full z-50 bg-indigo-900">
    <div class="max-w-6xl mx-auto px-6 py-4 flex justify-center gap-8 text-white">
        <a href="{{ route('home') }}">Home</a>
        <a href="{{ route('gallery') }}">Gallery</a>
        <a href="{{ route('paket') }}">Pricelist</a>
        <a href="{{ route('kontak') }}">Contact</a>
    </div>
</nav>
